MODULO2, LIBS INSTALADAS

yarn add nodemon
yarn add express
yarn add nunjucks

yarn add sequelize (ORM de BANCO RELACIONAL para NODE, trabalha com PostgreSQL, MySQL, SQLite and MSSQL)
yarn add sequelize-cli
yarn add pg (node usar postgresql)

//comandos sequelize 
npx sequelize init
npx sequelize migration:create --name=create-users
npx sequelize db:migrate

yarn add bcryptjs

yarn add multer (trabalha com upload de arquivos)

yarn add express-session (trab com sessions)

yarn add session-file-store (armazena sessao em arquivo storage) ////esse ta com erro, usar o proximo
yarn add connect-loki (armazena sessao em arquivo storage)

yarn add connect-flash (cria flash messages, msg de alerta de error/sucess a serem mostradas após uma requisicao)

yarn add moment (para trabalhar com datas)






